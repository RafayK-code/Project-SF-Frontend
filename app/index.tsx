import { Text, View } from "react-native";
import Login from "./Login";

export default function Index() {
  return (
    <View
      style={{
        flex: 1,
      }}
    >
      <Login></Login>
    </View>
  );
}
